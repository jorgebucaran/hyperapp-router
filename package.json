{
  "name": "@hyperapp/router",
  "description":
    "Actions and components for routing client-side pages with HyperApp.",
  "version": "0.1.0",
  "main": "dist/router.js",
  "jsnext:main": "src/index.js",
  "module": "src/index.js",
  "license": "MIT",
  "repository": "hyperapp/router",
  "files": ["src", "dist"],
  "author": "Luke Jackson (lukejacksonn@gmail.com)",
  "keywords": ["hyperapp", "router"],
  "scripts": {
    "build": "npm run bundle && npm run compile && npm run minify",
    "compile": "babel dist/router.js --out-file dist/router.js --presets=env",
    "bundle":
      "rollup -i src/index.js -g hyperapp:hyperapp -o dist/router.js -f umd -mn router",
    "minify":
      "uglifyjs dist/router.js -o dist/router.js --mangle --compress warnings=false --pure-funcs=Object.defineProperty -p relative --in-source-map dist/router.js.map --source-map dist/router.js.map",
    "format":
      "prettier --semi false --write 'src/**/*.js' '{,test/ts/}*.{ts,tsx}'",
    "release":
      "npm run build && npm test && git commit -am $npm_package_version && git tag $npm_package_version && git push && git push --tags && npm publish"
  },
  "babel": {
    "presets": "env"
  },
  "peerDependencies": {
    "hyperapp": "^0.16.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "1.6.1",
    "hyperapp": "^0.16.0",
    "jest": "^21.2.1",
    "prettier": "^1.8.2",
    "rollup": "^0.51.2",
    "uglify-js": "^2.7.5"
  }
}
