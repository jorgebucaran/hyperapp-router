{
  "name": "@hyperapp/router",
  "description":
    "Actions and components for routing client-side pages with HyperApp.",
  "version": "0.1.0",
  "main": "dist/router.js",
  "jsnext:main": "src/index.js",
  "module": "src/index.js",
  "license": "MIT",
  "repository": "hyperapp/router",
  "files": ["src", "dist"],
  "author": "Luke Jackson",
  "keywords": ["hyperapp", "router"],
  "scripts": {
    "build":
      "rollup -i src/index.js -g hyperapp:hyperapp -o dist/router.js -f umd -mn router",
    "release":
      "npm run build && npm test && git commit -am $npm_package_version && git tag $npm_package_version && git push && git push --tags && npm publish"
  },
  "peerDependencies": {
    "hyperapp": "^0.16.0"
  },
  "devDependencies": {
    "hyperapp": "^0.16.0",
    "rollup": "^0.50.0"
  }
}
